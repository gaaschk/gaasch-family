# ─── Local development ───────────────────────────────────────────────────────

# SQLite database path (relative path works for local dev)
DATABASE_URL="file:./prisma/dev.db"

# Auth.js secret — generate with: openssl rand -hex 32
AUTH_SECRET="<your-secret-here>"

# Base URL for this app
AUTH_URL="http://localhost:3000"

# Email — use https://ethereal.email for local testing (emails never actually sent)
# Ethereal:  smtp://user:pass@smtp.ethereal.email:587
# Gmail:     smtp://you@gmail.com:app-password@smtp.gmail.com:587
# Resend:    smtp://resend:api-key@smtp.resend.com:465
EMAIL_SERVER="smtp://user:password@smtp.ethereal.email:587"
EMAIL_FROM="Gaasch Family <noreply@gaasch.family>"

# ─── Production (Lightsail) — filled in automatically by server-setup.sh ─────
# DATABASE_URL="file:/home/ubuntu/gaasch-family/prisma/dev.db"
# AUTH_SECRET="<generated on server>"
# AUTH_URL="https://your-domain.com"
# EMAIL_SERVER="smtp://..."
# EMAIL_FROM="Gaasch Family <noreply@your-domain.com>"
